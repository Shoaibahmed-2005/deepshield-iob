# Core web framework
fastapi>=0.109.0
uvicorn>=0.27.0

# Database
sqlalchemy>=2.0.25
psycopg2-binary>=2.9.9

# Auth
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt==4.0.1          # passlib 1.7.4 is incompatible with bcrypt >= 4.1

# Multipart + env
python-multipart>=0.0.6
python-dotenv>=1.0.0

# Schema validation
pydantic>=2.5.3

# Image processing (for deepfake frame analysis)
pillow>=10.2.0
numpy>=1.26.3

# Deepfake detection model (large download — ~2.5 GB total)
# Install separately: pip install transformers torch
# transformers>=4.37.0
# torch>=2.1.0
