# Core web framework
fastapi>=0.109.0
uvicorn>=0.27.0

# Database
sqlalchemy>=2.0.25
psycopg2-binary>=2.9.9

# Auth
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt==4.0.1          # passlib 1.7.4 is incompatible with bcrypt >= 4.1

# Multipart + env
python-multipart>=0.0.6
python-dotenv>=1.0.0

# Schema validation
pydantic>=2.5.3

# Image processing (for deepfake frame analysis)
pillow>=10.2.0
numpy>=1.26.3

# Deepfake detection — CPU-only, no CUDA / PyTorch / TensorFlow required
# onnxruntime : ~35 MB wheel, installs in seconds on Windows/Linux/Mac.
# huggingface_hub : downloads the ONNX model file (~85 MB) on first run
#                   and caches it in %USERPROFILE%\.cache\huggingface\.
# Model: prithivMLmods/Deepfake-Detection-Exp-02-22-ONNX (INT8 ViT-B/32)
onnxruntime>=1.17.0
huggingface_hub>=0.20.0
